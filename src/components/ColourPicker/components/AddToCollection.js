import React from 'react'

const AddToCollection = ({handleSubmit}) => (
  <button className="btn btn-default btn-block" onClick={handleSubmit}>Add to Collection</button>
)

export default AddToCollection
